<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ahao.forum.guitar.module.post.dao.PostMapper">

    <select id="getPostByCategoryId" resultType="DataSet">
        select p.id, p.title, u.username
        from post p
        left join category c on c.id = p.category_id
        left join admin_user u on p.user_id = u.id
        where c.id = #{categoryId}
        <if test="search != null and search != ''">
            <bind name="searchLike" value="'%'+search+'%'"/>
            and (p.title like ${searchLike} or p.content like ${searchLike})
        </if>
    </select>
</mapper>