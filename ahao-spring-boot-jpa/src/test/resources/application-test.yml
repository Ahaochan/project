spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ahaodb;DB_CLOSE_DELAY=-1;MODE=MySQL;
    username: root
    password: root
  jpa:
    database: h2
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: none
